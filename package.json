{
  "name": "paras-vesting-contract",
  "version": "1.0.0",
  "description": "Paras vesting contract",
  "scripts": {
    "build": "cargo build --all --target wasm32-unknown-unknown --release",
    "postbuild": "cp target/wasm32-unknown-unknown/release/paras_vesting_contract.wasm ./res/",
    "dev": "near dev-deploy --wasmFile ./res/paras_vesting_contract.wasm"
  },
  "keywords": [
    "near-protocol",
    "blockchain",
    "rust",
    "smart-contract"
  ],
  "author": "Paras",
  "license": "(MIT AND Apache-2.0)",
  "devDependencies": {}
}
